## 2-7장

### 알게된 점

1. alert은 전달받은 값의 자료형과 관계없이 이를 문자열로 자동 변환하여 보여주는것이나 수학 관련 연산자가 전달 받은 값을 숫자로 변환하는 경우 형변환에 대표적인 예시.
2. let value = true => alert(value) => boolean 으로 뜬다. (문자열로 변환)
3. 문자이지만 안에 "3" / "1" 이런식으로 넣어도 계산해준다.
   4.Number 함수안에 숫자외에 문자가 들어가면 NaN이 뜬다. true = 1 false = 0
4. Boolean은 숫자로 변환 (1 =true 0 = false) 문자열은 leangh < 1 true 아예없으면 false
